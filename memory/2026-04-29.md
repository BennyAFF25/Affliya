# 2026-04-29

## Durable session memory (payment audit + sandbox setup)

- User shifted strategy from "full A–G in one pass" to **checkpointed execution** for reliability and visibility.
- **Checkpoint 1** was completed as a repo/db inspection only (no live tests/no writes): mapped Stripe/payment routes, env vars, frontend callers, Supabase read/write tables, Stripe object usage, and domain classification.
- Confirmed active Supabase test project switched to:
  - `gpaccxkfvcxzzpilsjww` (`https://gpaccxkfvcxzzpilsjww.supabase.co`)
- Confirmed service-role access works on this project and required payment-related tables are present.
- User explicitly requested strict operating rules for later checkpoints:
  - no code edits unless requested,
  - no writes to non-test rows without explicit approval,
  - maintain a row-level ledger for any writes (table, row id, owner/email, Stripe object id, delete/keep intent).
- Known architectural/route caveats identified in checkpoint mapping:
  - dual top-up ingestion paths exist (`/api/stripe/webhook` and `/api/stripe-session`), canonical path needs confirmation,
  - `wallet_refunds` table exists but current refund route behavior may center on `wallet_topups` updates,
  - business connect has both `create-account` and a disabled-style `create-business-account-link` path.
- User asked about repeated "API out of credits" message; likely root cause explained as model/provider quota/billing rather than Stripe/Supabase token issues.

## User preference/process memory

- User wants short, visible progress updates while long tasks run; lack of visible "typing" caused frustration.
- User prefers concrete, staged checkpoint approvals before moving to next phase.
