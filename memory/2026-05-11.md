# 2026-05-11

- Launch-readiness hardening advanced through LR-006 in `~/repos/Affliya`.
- LR-004 was applied live to Supabase via `supabase db query --linked -f supabase/migrations/20260511_ad_spend_settlement_atomic.sql` because `supabase db push` hit older duplicate migration version history (`20260304`).
- Live LR-004 smoke testing found a real schema mismatch: the atomic settlement function referenced `wallet_deductions.reason`, but the live table did not have that column. The DB function was fixed live and the repo migration/report were updated to match.
- LR-004 verified live: `settle_live_ad_spend` exists, settlement is atomic/idempotent, repeat settlement did not double-charge, and route-level post-settlement behavior was clean.
- LR-005 was completed and applied live with `supabase/migrations/20260511051500_wallet_refund_ledger.sql`.
- LR-005 introduced canonical refund ledger recording through `record_wallet_refund(...)`, added `wallet_refunds.source_topup_id`, unique refund-id protection, and updated `app/api/stripe/refund/route.ts` to honor the selected top-up from the wallet UI.
- LR-006 was completed and applied live with `supabase/migrations/20260511054500_canonical_conversion_payouts.sql`.
- LR-006 made `app/api/process-conversion/route.ts` the single canonical payout creator, removed direct payout/payment side effects from `app/api/track-event/route.ts` and `app/api/track.gif/route.ts`, and added DB idempotency via `create_wallet_payouts_for_conversion(...)` plus unique index `wallet_payouts_source_event_cycle_unique`.
- `process-conversion` was updated to resolve both `live_campaigns.id` and `live_ads.id` so paid Meta conversion flows keep working during the canonical payout cutover.
- Current next patch after compaction: LR-007 hard approval enforcement at money boundaries.
