# 2026-05-12

- LR-010 was completed in `~/repos/Affliya`.
- `app/affiliate/wallet/page.tsx` now shows truthful wallet UX: available balance, refundable-now, refund-locked amount, active Meta ad count, unsettled ad spend, and payout receipt visibility.
- Wallet ledger UX now includes top-ups, refunds, ad spend settlements/deductions, and affiliate payout receipts/scheduled payouts.
- Refund lock messaging is now explicit on the wallet page so users can see whether refunds are blocked by active Meta ads or unsettled spend.
- LR-010 verification passed targeted eslint for the wallet page; repo-wide TypeScript still has unrelated pre-existing failures outside the wallet page.
- LR-011 was completed in `~/repos/Affliya`.
- `app/api/run-payout/route.ts` now uses Stripe idempotency keys derived from payout ids, persists payment-intent / charge / transfer refs, and returns a repairable conflict if a payout is marked completed without Stripe refs.
- Business payout UI consumers were updated to treat `completed` as the canonical successful payout state while remaining compatible with older `paid` rows.
- Verified LR-011 end-to-end against Stripe test mode + linked Supabase test project with a synthetic payout: first run completed, second run returned `alreadyCompleted`, and a synthetic broken completed payout returned repairable HTTP 409.
- LR-012 was completed in `~/repos/Affliya`.
- Added shared commercial Stripe helpers in `utils/stripe.ts` so the wallet/refund/payout/settlement route set now shares one Stripe API version and one metadata builder.
- Standardized Stripe metadata around `nettmark_platform` + `nettmark_action` across wallet top-ups, refunds, payouts, and ad-spend settlements.
- Clarified ownership: `/api/stripe/webhook` is the sole wallet-topup crediting path; `/api/stripe-session` GET remains session lookup only and POST is now an explicit deprecated no-op.
- Verified LR-012 in Stripe test mode by creating a real top-up checkout session and retrieving its metadata, then executing a fresh synthetic payout and confirming normalized metadata on both the payment intent and transfer.
- LR-013 was completed in `~/repos/Affliya`.
- Added launch-safe stable identity refs across money-critical tables: `business_id` plus `affiliate_user_id` (the latter used because `campaign_tracking_events.affiliate_id` already exists as a legacy text snapshot field).
- Added DB-side auto-resolution/backfill triggers so existing email-based writers now populate stable IDs on insert/update without requiring a risky full route rewrite.
- Updated payout + ad-spend settlement runtime lookups to prefer stable IDs when present and fall back to email snapshots for compatibility.
- Verified LR-013 in the linked Supabase test project by inserting fresh top-up / payout / deduction / campaign / tracking rows and confirming stable IDs were auto-populated, then executed a real Stripe test-mode payout against a stable-ID-backed payout row.
- LR-015 was completed in `~/repos/Affliya`.
- Added a durable `money_flow_audit_log` table plus `utils/moneyFlowAudit.ts` so critical money decisions now leave queryable structured breadcrumbs instead of relying on scattered console output.
- Wired audit breadcrumbs into conversion quarantine, conversion payout creation, payout execution, ad-spend settlement, refund blocks/refunds, and Meta low-wallet auto-pause.
- Applied `supabase/migrations/20260512123000_money_flow_audit_log.sql` to the linked Supabase test project successfully.
- Verified live audit rows landed for a blocked refund (`NO_REFUNDABLE_TOPUP`), an invalid billable conversion quarantine (`INVALID_BILLABLE_CAMPAIGN_IDENTITY`), and a real Stripe test-mode payout execution (`wallet_payout_executed` for payout `2cfc4f59-1edf-4253-8593-fdec3c6b3b7f`).
- LR-014 was completed in `~/repos/Affliya`.
- Reduced `app/api/track.gif/route.ts` to a thin legacy GIF shim that proxies to `/api/track-event` and always returns a 1×1 GIF for compatibility.
- `track.gif` no longer owns campaign resolution, event insertion, quarantine handling, or `/api/process-conversion` handoff directly, so legacy callers cannot originate money movement outside the canonical `/api/track-event` → `/api/process-conversion` path.
- Verified the legacy route still returns a valid 1×1 GIF and still ingests a page-view event through the canonical path, and that an invalid legacy conversion request now quarantines via `app/api/track-event/route.ts` instead of legacy-route logic.
# 2026-05-12

- LR-010 was completed in `~/repos/Affliya`.
- `app/affiliate/wallet/page.tsx` now shows truthful wallet UX: available balance, refundable-now, refund-locked amount, active Meta ad count, unsettled ad spend, and payout receipt visibility.
- Wallet ledger UX now includes top-ups, refunds, ad spend settlements/deductions, and affiliate payout receipts/scheduled payouts.
- Refund lock messaging is now explicit on the wallet page so users can see whether refunds are blocked by active Meta ads or unsettled spend.
- LR-010 verification passed targeted eslint for the wallet page; repo-wide TypeScript still has unrelated pre-existing failures outside the wallet page.
- LR-011 was completed in `~/repos/Affliya`.
- `app/api/run-payout/route.ts` now uses Stripe idempotency keys derived from payout ids, persists payment-intent / charge / transfer refs, and returns a repairable conflict if a payout is marked completed without Stripe refs.
- Business payout UI consumers were updated to treat `completed` as the canonical successful payout state while remaining compatible with older `paid` rows.
- Verified LR-011 end-to-end against Stripe test mode + linked Supabase test project with a synthetic payout: first run completed, second run returned `alreadyCompleted`, and a synthetic broken completed payout returned repairable HTTP 409.
- LR-012 was completed in `~/repos/Affliya`.
- Added shared commercial Stripe helpers in `utils/stripe.ts` so the wallet/refund/payout/settlement route set now shares one Stripe API version and one metadata builder.
- Standardized Stripe metadata around `nettmark_platform` + `nettmark_action` across wallet top-ups, refunds, payouts, and ad-spend settlements.
- Clarified ownership: `/api/stripe/webhook` is the sole wallet-topup crediting path; `/api/stripe-session` GET remains session lookup only and POST is now an explicit deprecated no-op.
- Verified LR-012 in Stripe test mode by creating a real top-up checkout session and retrieving its metadata, then executing a fresh synthetic payout and confirming normalized metadata on both the payment intent and transfer.
- LR-013 was completed in `~/repos/Affliya`.
- Added launch-safe stable identity refs across money-critical tables: `business_id` plus `affiliate_user_id` (the latter used because `campaign_tracking_events.affiliate_id` already exists as a legacy text snapshot field).
- Added DB-side auto-resolution/backfill triggers so existing email-based writers now populate stable IDs on insert/update without requiring a risky full route rewrite.
- Updated payout + ad-spend settlement runtime lookups to prefer stable IDs when present and fall back to email snapshots for compatibility.
- Verified LR-013 in the linked Supabase test project by inserting fresh top-up / payout / deduction / campaign / tracking rows and confirming stable IDs were auto-populated, then executed a real Stripe test-mode payout against a stable-ID-backed payout row.
- LR-015 was completed in `~/repos/Affliya`.
- Added a durable `money_flow_audit_log` table plus `utils/moneyFlowAudit.ts` so critical money decisions now leave queryable structured breadcrumbs instead of relying on scattered console output.
- Wired audit breadcrumbs into conversion quarantine, conversion payout creation, payout execution, ad-spend settlement, refund blocks/refunds, and Meta low-wallet auto-pause.
- Applied `supabase/migrations/20260512123000_money_flow_audit_log.sql` to the linked Supabase test project successfully.
- Verified live audit rows landed for a blocked refund (`NO_REFUNDABLE_TOPUP`), an invalid billable conversion quarantine (`INVALID_BILLABLE_CAMPAIGN_IDENTITY`), and a real Stripe test-mode payout execution (`wallet_payout_executed` for payout `2cfc4f59-1edf-4253-8593-fdec3c6b3b7f`).
- LR-014 was completed in `~/repos/Affliya`.
- Reduced `app/api/track.gif/route.ts` to a thin legacy GIF shim that proxies to `/api/track-event` and always returns a 1×1 GIF for compatibility.
- `track.gif` no longer owns campaign resolution, event insertion, quarantine handling, or `/api/process-conversion` handoff directly, so legacy callers cannot originate money movement outside the canonical `/api/track-event` → `/api/process-conversion` path.
- Verified the legacy route still returns a valid 1×1 GIF and still ingests a page-view event through the canonical path, and that an invalid legacy conversion request now quarantines via `app/api/track-event/route.ts` instead of legacy-route logic.
- Full staging money-flow probing found that `POST /api/ad-spend/settle` succeeded on live ad `89783681-a2a5-4389-a150-10fe47826651`, creating wallet deduction `acc7b211-108d-42e1-b271-0b20b0030807` and Stripe transfer `tr_1TW5krADLjZ5ptV0F2jJxeuJ` for charged spend `17.34`.
- A real canonical conversion test inserted `campaign_tracking_events` row `a7c0355b-776e-43d5-a7df-febca20d102e`; `POST /api/process-conversion` created affiliate payout `37.5` at 25% commission and produced payout row `bbcc7d08-13a3-4478-9a51-ed70a78506c6`.
- The main staging blocker is Stripe mode mismatch during `POST /api/run-payout`: the business record used live-mode customer id `cus_TonZpzJX379sLA` while the route executed with Stripe test-mode keys, so payout execution failed before charging the business.
- Current affiliate wallet "withdrawal" is not a standalone withdrawal system; it is `POST /api/stripe/refund` against refundable top-ups and is intentionally locked while active Meta ads or unsettled spend remain.
- Refund-lock inspection showed the tested affiliate currently had `ACTIVE_META_AD_LOCK` with 4 active ads and 18.91 unsettled spend, so refund/withdrawal was correctly blocked.
- There is a payout-failure bookkeeping gap: after the Stripe customer mismatch, payout `bbcc7d08-13a3-4478-9a51-ed70a78506c6` remained pending even though audit logged `wallet_payout_execution_failed`, indicating the unhandled failure path is not reliably marking the payout row failed.
- A fresh Stripe Checkout session for wallet top-up was created successfully, but final payment submission/webhook crediting was not completed in that run.
# 2026-05-12

- LR-010 was completed in `~/repos/Affliya`.
- `app/affiliate/wallet/page.tsx` now shows truthful wallet UX: available balance, refundable-now, refund-locked amount, active Meta ad count, unsettled ad spend, and payout receipt visibility.
- Wallet ledger UX now includes top-ups, refunds, ad spend settlements/deductions, and affiliate payout receipts/scheduled payouts.
- Refund lock messaging is now explicit on the wallet page so users can see whether refunds are blocked by active Meta ads or unsettled spend.
- LR-010 verification passed targeted eslint for the wallet page; repo-wide TypeScript still has unrelated pre-existing failures outside the wallet page.
- LR-011 was completed in `~/repos/Affliya`.
- `app/api/run-payout/route.ts` now uses Stripe idempotency keys derived from payout ids, persists payment-intent / charge / transfer refs, and returns a repairable conflict if a payout is marked completed without Stripe refs.
- Business payout UI consumers were updated to treat `completed` as the canonical successful payout state while remaining compatible with older `paid` rows.
- Verified LR-011 end-to-end against Stripe test mode + linked Supabase test project with a synthetic payout: first run completed, second run returned `alreadyCompleted`, and a synthetic broken completed payout returned repairable HTTP 409.
- LR-012 was completed in `~/repos/Affliya`.
- Added shared commercial Stripe helpers in `utils/stripe.ts` so the wallet/refund/payout/settlement route set now shares one Stripe API version and one metadata builder.
- Standardized Stripe metadata around `nettmark_platform` + `nettmark_action` across wallet top-ups, refunds, payouts, and ad-spend settlements.
- Clarified ownership: `/api/stripe/webhook` is the sole wallet-topup crediting path; `/api/stripe-session` GET remains session lookup only and POST is now an explicit deprecated no-op.
- Verified LR-012 in Stripe test mode by creating a real top-up checkout session and retrieving its metadata, then executing a fresh synthetic payout and confirming normalized metadata on both the payment intent and transfer.
- LR-013 was completed in `~/repos/Affliya`.
- Added launch-safe stable identity refs across money-critical tables: `business_id` plus `affiliate_user_id` (the latter used because `campaign_tracking_events.affiliate_id` already exists as a legacy text snapshot field).
- Added DB-side auto-resolution/backfill triggers so existing email-based writers now populate stable IDs on insert/update without requiring a risky full route rewrite.
- Updated payout + ad-spend settlement runtime lookups to prefer stable IDs when present and fall back to email snapshots for compatibility.
- Verified LR-013 in the linked Supabase test project by inserting fresh top-up / payout / deduction / campaign / tracking rows and confirming stable IDs were auto-populated, then executed a real Stripe test-mode payout against a stable-ID-backed payout row.
- LR-015 was completed in `~/repos/Affliya`.
- Added a durable `money_flow_audit_log` table plus `utils/moneyFlowAudit.ts` so critical money decisions now leave queryable structured breadcrumbs instead of relying on scattered console output.
- Wired audit breadcrumbs into conversion quarantine, conversion payout creation, payout execution, ad-spend settlement, refund blocks/refunds, and Meta low-wallet auto-pause.
- Applied `supabase/migrations/20260512123000_money_flow_audit_log.sql` to the linked Supabase test project successfully.
- Verified live audit rows landed for a blocked refund (`NO_REFUNDABLE_TOPUP`), an invalid billable conversion quarantine (`INVALID_BILLABLE_CAMPAIGN_IDENTITY`), and a real Stripe test-mode payout execution (`wallet_payout_executed` for payout `2cfc4f59-1edf-4253-8593-fdec3c6b3b7f`).
- LR-014 was completed in `~/repos/Affliya`.
- Reduced `app/api/track.gif/route.ts` to a thin legacy GIF shim that proxies to `/api/track-event` and always returns a 1×1 GIF for compatibility.
- `track.gif` no longer owns campaign resolution, event insertion, quarantine handling, or `/api/process-conversion` handoff directly, so legacy callers cannot originate money movement outside the canonical `/api/track-event` → `/api/process-conversion` path.
- Verified the legacy route still returns a valid 1×1 GIF and still ingests a page-view event through the canonical path, and that an invalid legacy conversion request now quarantines via `app/api/track-event/route.ts` instead of legacy-route logic.
# 2026-05-12

- LR-010 was completed in `~/repos/Affliya`.
- `app/affiliate/wallet/page.tsx` now shows truthful wallet UX: available balance, refundable-now, refund-locked amount, active Meta ad count, unsettled ad spend, and payout receipt visibility.
- Wallet ledger UX now includes top-ups, refunds, ad spend settlements/deductions, and affiliate payout receipts/scheduled payouts.
- Refund lock messaging is now explicit on the wallet page so users can see whether refunds are blocked by active Meta ads or unsettled spend.
- LR-010 verification passed targeted eslint for the wallet page; repo-wide TypeScript still has unrelated pre-existing failures outside the wallet page.
- LR-011 was completed in `~/repos/Affliya`.
- `app/api/run-payout/route.ts` now uses Stripe idempotency keys derived from payout ids, persists payment-intent / charge / transfer refs, and returns a repairable conflict if a payout is marked completed without Stripe refs.
- Business payout UI consumers were updated to treat `completed` as the canonical successful payout state while remaining compatible with older `paid` rows.
- Verified LR-011 end-to-end against Stripe test mode + linked Supabase test project with a synthetic payout: first run completed, second run returned `alreadyCompleted`, and a synthetic broken completed payout returned repairable HTTP 409.
- LR-012 was completed in `~/repos/Affliya`.
- Added shared commercial Stripe helpers in `utils/stripe.ts` so the wallet/refund/payout/settlement route set now shares one Stripe API version and one metadata builder.
- Standardized Stripe metadata around `nettmark_platform` + `nettmark_action` across wallet top-ups, refunds, payouts, and ad-spend settlements.
- Clarified ownership: `/api/stripe/webhook` is the sole wallet-topup crediting path; `/api/stripe-session` GET remains session lookup only and POST is now an explicit deprecated no-op.
- Verified LR-012 in Stripe test mode by creating a real top-up checkout session and retrieving its metadata, then executing a fresh synthetic payout and confirming normalized metadata on both the payment intent and transfer.
- LR-013 was completed in `~/repos/Affliya`.
- Added launch-safe stable identity refs across money-critical tables: `business_id` plus `affiliate_user_id` (the latter used because `campaign_tracking_events.affiliate_id` already exists as a legacy text snapshot field).
- Added DB-side auto-resolution/backfill triggers so existing email-based writers now populate stable IDs on insert/update without requiring a risky full route rewrite.
- Updated payout + ad-spend settlement runtime lookups to prefer stable IDs when present and fall back to email snapshots for compatibility.
- Verified LR-013 in the linked Supabase test project by inserting fresh top-up / payout / deduction / campaign / tracking rows and confirming stable IDs were auto-populated, then executed a real Stripe test-mode payout against a stable-ID-backed payout row.
- LR-015 was completed in `~/repos/Affliya`.
- Added a durable `money_flow_audit_log` table plus `utils/moneyFlowAudit.ts` so critical money decisions now leave queryable structured breadcrumbs instead of relying on scattered console output.
- Wired audit breadcrumbs into conversion quarantine, conversion payout creation, payout execution, ad-spend settlement, refund blocks/refunds, and Meta low-wallet auto-pause.
- Applied `supabase/migrations/20260512123000_money_flow_audit_log.sql` to the linked Supabase test project successfully.
- Verified live audit rows landed for a blocked refund (`NO_REFUNDABLE_TOPUP`), an invalid billable conversion quarantine (`INVALID_BILLABLE_CAMPAIGN_IDENTITY`), and a real Stripe test-mode payout execution (`wallet_payout_executed` for payout `2cfc4f59-1edf-4253-8593-fdec3c6b3b7f`).
- LR-014 was completed in `~/repos/Affliya`.
- Reduced `app/api/track.gif/route.ts` to a thin legacy GIF shim that proxies to `/api/track-event` and always returns a 1×1 GIF for compatibility.
- `track.gif` no longer owns campaign resolution, event insertion, quarantine handling, or `/api/process-conversion` handoff directly, so legacy callers cannot originate money movement outside the canonical `/api/track-event` → `/api/process-conversion` path.
- Verified the legacy route still returns a valid 1×1 GIF and still ingests a page-view event through the canonical path, and that an invalid legacy conversion request now quarantines via `app/api/track-event/route.ts` instead of legacy-route logic.
- Full staging money-flow probing found that `POST /api/ad-spend/settle` succeeded on live ad `89783681-a2a5-4389-a150-10fe47826651`, creating wallet deduction `acc7b211-108d-42e1-b271-0b20b0030807` and Stripe transfer `tr_1TW5krADLjZ5ptV0F2jJxeuJ` for charged spend `17.34`.
- A real canonical conversion test inserted `campaign_tracking_events` row `a7c0355b-776e-43d5-a7df-febca20d102e`; `POST /api/process-conversion` created affiliate payout `37.5` at 25% commission and produced payout row `bbcc7d08-13a3-4478-9a51-ed70a78506c6`.
- The main staging blocker is Stripe mode mismatch during `POST /api/run-payout`: the business record used live-mode customer id `cus_TonZpzJX379sLA` while the route executed with Stripe test-mode keys, so payout execution failed before charging the business.
- Current affiliate wallet "withdrawal" is not a standalone withdrawal system; it is `POST /api/stripe/refund` against refundable top-ups and is intentionally locked while active Meta ads or unsettled spend remain.
- Refund-lock inspection showed the tested affiliate currently had `ACTIVE_META_AD_LOCK` with 4 active ads and 18.91 unsettled spend, so refund/withdrawal was correctly blocked.
- There is a payout-failure bookkeeping gap: after the Stripe customer mismatch, payout `bbcc7d08-13a3-4478-9a51-ed70a78506c6` remained pending even though audit logged `wallet_payout_execution_failed`, indicating the unhandled failure path is not reliably marking the payout row failed.
- A fresh Stripe Checkout session for wallet top-up was created successfully, but final payment submission/webhook crediting was not completed in that run.
- Hydration mismatches during affiliate Stripe onboarding traced to theme-dependent SSR/client differences in `context/ThemeContext.tsx`, `app/components/marketing/MarketingHeader.tsx`, and `app/components/Topbar.tsx`; fixes made first render deterministic by deferring browser theme preference reads until after mount.
- Console noise during affiliate wallet testing surfaced two actionable issues: `wallet_deductions.reason` was queried from `app/affiliate/wallet/page.tsx` even though that column does not exist, and `POST /api/stripe/affiliates/create-account` was returning 500 during payout onboarding.
- Affiliate Stripe onboarding route hardening work included avoiding localhost as Stripe `business_profile.url`, keeping local refresh/return URLs separate, and improving error detail propagation for the create-account path so future failures are easier to diagnose.
- Fixed business signup trigger/RLS failure by changing `public.ensure_business_profile()` to `SECURITY DEFINER` and applying `supabase/migrations/20260512163000_business_profile_trigger_security_definer.sql` to the linked DB.
- Full test-pair money-flow probing used `biz@testuser.com` and `affiliate@testuser.com` with both Stripe onboardings complete; synthetic signed webhook testing proved `app/api/stripe/webhook/route.ts` was dropping valid top-ups when `latest_charge.balance_transaction` was not ready on the first fetch.
- Hardened `app/api/stripe/webhook/route.ts` with retry + fallback resolution of the top-up charge/balance-transaction path; verified a 25 AUD synthetic top-up now records `wallet_topups` row `81cb80f6-f09f-410e-99d5-e60f33128cc7` and updates affiliate wallet balance to 23.82.
- Confirmed canonical refund path bug: wallet top-ups store `stripe_id` as Checkout Session ids (`cs_...`) but `app/api/stripe/refund/route.ts` only accepted `ch_...` / `pi_...`; patched route to resolve `cs_...` -> Checkout Session -> PaymentIntent -> Charge before refund creation.
- Confirmed payout execution bookkeeping gap for transfer failures on the test pair: `POST /api/run-payout` successfully charged the business but failed the affiliate transfer due Stripe test-mode available-balance limits, leaving payout `aa8a13fa-8bef-4e51-887f-fde37764e8cf` pending with charge refs. Patched `app/api/run-payout/route.ts` so transfer failures now return `409 transfer_failed` and persist failed state; verified on payout `5b4e91c1-db4f-45e7-ad78-2364683579fd`.
- Verified direct conversion sequencing for the test pair: seeded approved affiliate request, inserted conversion event `a7b014c1-4381-43ea-bc6f-450ab640f24e`, and `POST /api/process-conversion` created exactly one pending payout `d7ba2a74-5230-462e-bc16-a01e035775e5` for 22 AUD at 22% commission; repeat call returned `already_processed` with payout count still 1.
- Verified direct ad-spend settlement sequencing for the test pair: settling live ad `d7b76986-bf98-40aa-a34e-4fd3a9c711c6` created deduction `71629177-ac7f-4b5f-b030-3f4dd160af99`, advanced `spend_transferred` from 0 to 5, and canonical wallet math dropped available balance from 23.82 to 18.82; repeat settlement was idempotent and created no extra deductions.
- Important nuance from the test-pair audit: `wallets.balance` remained 23.82 after the ad-spend deduction, so any UI or logic that trusts the cached wallet row instead of canonical ledger snapshot will drift.
# 2026-05-12

- LR-010 was completed in `~/repos/Affliya`.
- `app/affiliate/wallet/page.tsx` now shows truthful wallet UX: available balance, refundable-now, refund-locked amount, active Meta ad count, unsettled ad spend, and payout receipt visibility.
- Wallet ledger UX now includes top-ups, refunds, ad spend settlements/deductions, and affiliate payout receipts/scheduled payouts.
- Refund lock messaging is now explicit on the wallet page so users can see whether refunds are blocked by active Meta ads or unsettled spend.
- LR-010 verification passed targeted eslint for the wallet page; repo-wide TypeScript still has unrelated pre-existing failures outside the wallet page.
- LR-011 was completed in `~/repos/Affliya`.
- `app/api/run-payout/route.ts` now uses Stripe idempotency keys derived from payout ids, persists payment-intent / charge / transfer refs, and returns a repairable conflict if a payout is marked completed without Stripe refs.
- Business payout UI consumers were updated to treat `completed` as the canonical successful payout state while remaining compatible with older `paid` rows.
- Verified LR-011 end-to-end against Stripe test mode + linked Supabase test project with a synthetic payout: first run completed, second run returned `alreadyCompleted`, and a synthetic broken completed payout returned repairable HTTP 409.
- LR-012 was completed in `~/repos/Affliya`.
- Added shared commercial Stripe helpers in `utils/stripe.ts` so the wallet/refund/payout/settlement route set now shares one Stripe API version and one metadata builder.
- Standardized Stripe metadata around `nettmark_platform` + `nettmark_action` across wallet top-ups, refunds, payouts, and ad-spend settlements.
- Clarified ownership: `/api/stripe/webhook` is the sole wallet-topup crediting path; `/api/stripe-session` GET remains session lookup only and POST is now an explicit deprecated no-op.
- Verified LR-012 in Stripe test mode by creating a real top-up checkout session and retrieving its metadata, then executing a fresh synthetic payout and confirming normalized metadata on both the payment intent and transfer.
- LR-013 was completed in `~/repos/Affliya`.
- Added launch-safe stable identity refs across money-critical tables: `business_id` plus `affiliate_user_id` (the latter used because `campaign_tracking_events.affiliate_id` already exists as a legacy text snapshot field).
- Added DB-side auto-resolution/backfill triggers so existing email-based writers now populate stable IDs on insert/update without requiring a risky full route rewrite.
- Updated payout + ad-spend settlement runtime lookups to prefer stable IDs when present and fall back to email snapshots for compatibility.
- Verified LR-013 in the linked Supabase test project by inserting fresh top-up / payout / deduction / campaign / tracking rows and confirming stable IDs were auto-populated, then executed a real Stripe test-mode payout against a stable-ID-backed payout row.
- LR-015 was completed in `~/repos/Affliya`.
- Added a durable `money_flow_audit_log` table plus `utils/moneyFlowAudit.ts` so critical money decisions now leave queryable structured breadcrumbs instead of relying on scattered console output.
- Wired audit breadcrumbs into conversion quarantine, conversion payout creation, payout execution, ad-spend settlement, refund blocks/refunds, and Meta low-wallet auto-pause.
- Applied `supabase/migrations/20260512123000_money_flow_audit_log.sql` to the linked Supabase test project successfully.
- Verified live audit rows landed for a blocked refund (`NO_REFUNDABLE_TOPUP`), an invalid billable conversion quarantine (`INVALID_BILLABLE_CAMPAIGN_IDENTITY`), and a real Stripe test-mode payout execution (`wallet_payout_executed` for payout `2cfc4f59-1edf-4253-8593-fdec3c6b3b7f`).
- LR-014 was completed in `~/repos/Affliya`.
- Reduced `app/api/track.gif/route.ts` to a thin legacy GIF shim that proxies to `/api/track-event` and always returns a 1×1 GIF for compatibility.
- `track.gif` no longer owns campaign resolution, event insertion, quarantine handling, or `/api/process-conversion` handoff directly, so legacy callers cannot originate money movement outside the canonical `/api/track-event` → `/api/process-conversion` path.
- Verified the legacy route still returns a valid 1×1 GIF and still ingests a page-view event through the canonical path, and that an invalid legacy conversion request now quarantines via `app/api/track-event/route.ts` instead of legacy-route logic.
# 2026-05-12

- LR-010 was completed in `~/repos/Affliya`.
- `app/affiliate/wallet/page.tsx` now shows truthful wallet UX: available balance, refundable-now, refund-locked amount, active Meta ad count, unsettled ad spend, and payout receipt visibility.
- Wallet ledger UX now includes top-ups, refunds, ad spend settlements/deductions, and affiliate payout receipts/scheduled payouts.
- Refund lock messaging is now explicit on the wallet page so users can see whether refunds are blocked by active Meta ads or unsettled spend.
- LR-010 verification passed targeted eslint for the wallet page; repo-wide TypeScript still has unrelated pre-existing failures outside the wallet page.
- LR-011 was completed in `~/repos/Affliya`.
- `app/api/run-payout/route.ts` now uses Stripe idempotency keys derived from payout ids, persists payment-intent / charge / transfer refs, and returns a repairable conflict if a payout is marked completed without Stripe refs.
- Business payout UI consumers were updated to treat `completed` as the canonical successful payout state while remaining compatible with older `paid` rows.
- Verified LR-011 end-to-end against Stripe test mode + linked Supabase test project with a synthetic payout: first run completed, second run returned `alreadyCompleted`, and a synthetic broken completed payout returned repairable HTTP 409.
- LR-012 was completed in `~/repos/Affliya`.
- Added shared commercial Stripe helpers in `utils/stripe.ts` so the wallet/refund/payout/settlement route set now shares one Stripe API version and one metadata builder.
- Standardized Stripe metadata around `nettmark_platform` + `nettmark_action` across wallet top-ups, refunds, payouts, and ad-spend settlements.
- Clarified ownership: `/api/stripe/webhook` is the sole wallet-topup crediting path; `/api/stripe-session` GET remains session lookup only and POST is now an explicit deprecated no-op.
- Verified LR-012 in Stripe test mode by creating a real top-up checkout session and retrieving its metadata, then executing a fresh synthetic payout and confirming normalized metadata on both the payment intent and transfer.
- LR-013 was completed in `~/repos/Affliya`.
- Added launch-safe stable identity refs across money-critical tables: `business_id` plus `affiliate_user_id` (the latter used because `campaign_tracking_events.affiliate_id` already exists as a legacy text snapshot field).
- Added DB-side auto-resolution/backfill triggers so existing email-based writers now populate stable IDs on insert/update without requiring a risky full route rewrite.
- Updated payout + ad-spend settlement runtime lookups to prefer stable IDs when present and fall back to email snapshots for compatibility.
- Verified LR-013 in the linked Supabase test project by inserting fresh top-up / payout / deduction / campaign / tracking rows and confirming stable IDs were auto-populated, then executed a real Stripe test-mode payout against a stable-ID-backed payout row.
- LR-015 was completed in `~/repos/Affliya`.
- Added a durable `money_flow_audit_log` table plus `utils/moneyFlowAudit.ts` so critical money decisions now leave queryable structured breadcrumbs instead of relying on scattered console output.
- Wired audit breadcrumbs into conversion quarantine, conversion payout creation, payout execution, ad-spend settlement, refund blocks/refunds, and Meta low-wallet auto-pause.
- Applied `supabase/migrations/20260512123000_money_flow_audit_log.sql` to the linked Supabase test project successfully.
- Verified live audit rows landed for a blocked refund (`NO_REFUNDABLE_TOPUP`), an invalid billable conversion quarantine (`INVALID_BILLABLE_CAMPAIGN_IDENTITY`), and a real Stripe test-mode payout execution (`wallet_payout_executed` for payout `2cfc4f59-1edf-4253-8593-fdec3c6b3b7f`).
- LR-014 was completed in `~/repos/Affliya`.
- Reduced `app/api/track.gif/route.ts` to a thin legacy GIF shim that proxies to `/api/track-event` and always returns a 1×1 GIF for compatibility.
- `track.gif` no longer owns campaign resolution, event insertion, quarantine handling, or `/api/process-conversion` handoff directly, so legacy callers cannot originate money movement outside the canonical `/api/track-event` → `/api/process-conversion` path.
- Verified the legacy route still returns a valid 1×1 GIF and still ingests a page-view event through the canonical path, and that an invalid legacy conversion request now quarantines via `app/api/track-event/route.ts` instead of legacy-route logic.
- Full staging money-flow probing found that `POST /api/ad-spend/settle` succeeded on live ad `89783681-a2a5-4389-a150-10fe47826651`, creating wallet deduction `acc7b211-108d-42e1-b271-0b20b0030807` and Stripe transfer `tr_1TW5krADLjZ5ptV0F2jJxeuJ` for charged spend `17.34`.
- A real canonical conversion test inserted `campaign_tracking_events` row `a7c0355b-776e-43d5-a7df-febca20d102e`; `POST /api/process-conversion` created affiliate payout `37.5` at 25% commission and produced payout row `bbcc7d08-13a3-4478-9a51-ed70a78506c6`.
- The main staging blocker is Stripe mode mismatch during `POST /api/run-payout`: the business record used live-mode customer id `cus_TonZpzJX379sLA` while the route executed with Stripe test-mode keys, so payout execution failed before charging the business.
- Current affiliate wallet "withdrawal" is not a standalone withdrawal system; it is `POST /api/stripe/refund` against refundable top-ups and is intentionally locked while active Meta ads or unsettled spend remain.
- Refund-lock inspection showed the tested affiliate currently had `ACTIVE_META_AD_LOCK` with 4 active ads and 18.91 unsettled spend, so refund/withdrawal was correctly blocked.
- There is a payout-failure bookkeeping gap: after the Stripe customer mismatch, payout `bbcc7d08-13a3-4478-9a51-ed70a78506c6` remained pending even though audit logged `wallet_payout_execution_failed`, indicating the unhandled failure path is not reliably marking the payout row failed.
- A fresh Stripe Checkout session for wallet top-up was created successfully, but final payment submission/webhook crediting was not completed in that run.
# 2026-05-12

- LR-010 was completed in `~/repos/Affliya`.
- `app/affiliate/wallet/page.tsx` now shows truthful wallet UX: available balance, refundable-now, refund-locked amount, active Meta ad count, unsettled ad spend, and payout receipt visibility.
- Wallet ledger UX now includes top-ups, refunds, ad spend settlements/deductions, and affiliate payout receipts/scheduled payouts.
- Refund lock messaging is now explicit on the wallet page so users can see whether refunds are blocked by active Meta ads or unsettled spend.
- LR-010 verification passed targeted eslint for the wallet page; repo-wide TypeScript still has unrelated pre-existing failures outside the wallet page.
- LR-011 was completed in `~/repos/Affliya`.
- `app/api/run-payout/route.ts` now uses Stripe idempotency keys derived from payout ids, persists payment-intent / charge / transfer refs, and returns a repairable conflict if a payout is marked completed without Stripe refs.
- Business payout UI consumers were updated to treat `completed` as the canonical successful payout state while remaining compatible with older `paid` rows.
- Verified LR-011 end-to-end against Stripe test mode + linked Supabase test project with a synthetic payout: first run completed, second run returned `alreadyCompleted`, and a synthetic broken completed payout returned repairable HTTP 409.
- LR-012 was completed in `~/repos/Affliya`.
- Added shared commercial Stripe helpers in `utils/stripe.ts` so the wallet/refund/payout/settlement route set now shares one Stripe API version and one metadata builder.
- Standardized Stripe metadata around `nettmark_platform` + `nettmark_action` across wallet top-ups, refunds, payouts, and ad-spend settlements.
- Clarified ownership: `/api/stripe/webhook` is the sole wallet-topup crediting path; `/api/stripe-session` GET remains session lookup only and POST is now an explicit deprecated no-op.
- Verified LR-012 in Stripe test mode by creating a real top-up checkout session and retrieving its metadata, then executing a fresh synthetic payout and confirming normalized metadata on both the payment intent and transfer.
- LR-013 was completed in `~/repos/Affliya`.
- Added launch-safe stable identity refs across money-critical tables: `business_id` plus `affiliate_user_id` (the latter used because `campaign_tracking_events.affiliate_id` already exists as a legacy text snapshot field).
- Added DB-side auto-resolution/backfill triggers so existing email-based writers now populate stable IDs on insert/update without requiring a risky full route rewrite.
- Updated payout + ad-spend settlement runtime lookups to prefer stable IDs when present and fall back to email snapshots for compatibility.
- Verified LR-013 in the linked Supabase test project by inserting fresh top-up / payout / deduction / campaign / tracking rows and confirming stable IDs were auto-populated, then executed a real Stripe test-mode payout against a stable-ID-backed payout row.
- LR-015 was completed in `~/repos/Affliya`.
- Added a durable `money_flow_audit_log` table plus `utils/moneyFlowAudit.ts` so critical money decisions now leave queryable structured breadcrumbs instead of relying on scattered console output.
- Wired audit breadcrumbs into conversion quarantine, conversion payout creation, payout execution, ad-spend settlement, refund blocks/refunds, and Meta low-wallet auto-pause.
- Applied `supabase/migrations/20260512123000_money_flow_audit_log.sql` to the linked Supabase test project successfully.
- Verified live audit rows landed for a blocked refund (`NO_REFUNDABLE_TOPUP`), an invalid billable conversion quarantine (`INVALID_BILLABLE_CAMPAIGN_IDENTITY`), and a real Stripe test-mode payout execution (`wallet_payout_executed` for payout `2cfc4f59-1edf-4253-8593-fdec3c6b3b7f`).
- LR-014 was completed in `~/repos/Affliya`.
- Reduced `app/api/track.gif/route.ts` to a thin legacy GIF shim that proxies to `/api/track-event` and always returns a 1×1 GIF for compatibility.
- `track.gif` no longer owns campaign resolution, event insertion, quarantine handling, or `/api/process-conversion` handoff directly, so legacy callers cannot originate money movement outside the canonical `/api/track-event` → `/api/process-conversion` path.
- Verified the legacy route still returns a valid 1×1 GIF and still ingests a page-view event through the canonical path, and that an invalid legacy conversion request now quarantines via `app/api/track-event/route.ts` instead of legacy-route logic.
- Full staging money-flow probing found that `POST /api/ad-spend/settle` succeeded on live ad `89783681-a2a5-4389-a150-10fe47826651`, creating wallet deduction `acc7b211-108d-42e1-b271-0b20b0030807` and Stripe transfer `tr_1TW5krADLjZ5ptV0F2jJxeuJ` for charged spend `17.34`.
- A real canonical conversion test inserted `campaign_tracking_events` row `a7c0355b-776e-43d5-a7df-febca20d102e`; `POST /api/process-conversion` created affiliate payout `37.5` at 25% commission and produced payout row `bbcc7d08-13a3-4478-9a51-ed70a78506c6`.
- The main staging blocker is Stripe mode mismatch during `POST /api/run-payout`: the business record used live-mode customer id `cus_TonZpzJX379sLA` while the route executed with Stripe test-mode keys, so payout execution failed before charging the business.
- Current affiliate wallet "withdrawal" is not a standalone withdrawal system; it is `POST /api/stripe/refund` against refundable top-ups and is intentionally locked while active Meta ads or unsettled spend remain.
- Refund-lock inspection showed the tested affiliate currently had `ACTIVE_META_AD_LOCK` with 4 active ads and 18.91 unsettled spend, so refund/withdrawal was correctly blocked.
- There is a payout-failure bookkeeping gap: after the Stripe customer mismatch, payout `bbcc7d08-13a3-4478-9a51-ed70a78506c6` remained pending even though audit logged `wallet_payout_execution_failed`, indicating the unhandled failure path is not reliably marking the payout row failed.
- A fresh Stripe Checkout session for wallet top-up was created successfully, but final payment submission/webhook crediting was not completed in that run.
- Hydration mismatches during affiliate Stripe onboarding traced to theme-dependent SSR/client differences in `context/ThemeContext.tsx`, `app/components/marketing/MarketingHeader.tsx`, and `app/components/Topbar.tsx`; fixes made first render deterministic by deferring browser theme preference reads until after mount.
- Console noise during affiliate wallet testing surfaced two actionable issues: `wallet_deductions.reason` was queried from `app/affiliate/wallet/page.tsx` even though that column does not exist, and `POST /api/stripe/affiliates/create-account` was returning 500 during payout onboarding.
- Affiliate Stripe onboarding route hardening work included avoiding localhost as Stripe `business_profile.url`, keeping local refresh/return URLs separate, and improving error detail propagation for the create-account path so future failures are easier to diagnose.
- Fixed business signup trigger/RLS failure by changing `public.ensure_business_profile()` to `SECURITY DEFINER` and applying `supabase/migrations/20260512163000_business_profile_trigger_security_definer.sql` to the linked DB.
- Full test-pair money-flow probing used `biz@testuser.com` and `affiliate@testuser.com` with both Stripe onboardings complete; synthetic signed webhook testing proved `app/api/stripe/webhook/route.ts` was dropping valid top-ups when `latest_charge.balance_transaction` was not ready on the first fetch.
- Hardened `app/api/stripe/webhook/route.ts` with retry + fallback resolution of the top-up charge/balance-transaction path; verified a 25 AUD synthetic top-up now records `wallet_topups` row `81cb80f6-f09f-410e-99d5-e60f33128cc7` and updates affiliate wallet balance to 23.82.
- Confirmed canonical refund path bug: wallet top-ups store `stripe_id` as Checkout Session ids (`cs_...`) but `app/api/stripe/refund/route.ts` only accepted `ch_...` / `pi_...`; patched route to resolve `cs_...` -> Checkout Session -> PaymentIntent -> Charge before refund creation.
- Confirmed payout execution bookkeeping gap for transfer failures on the test pair: `POST /api/run-payout` successfully charged the business but failed the affiliate transfer due Stripe test-mode available-balance limits, leaving payout `aa8a13fa-8bef-4e51-887f-fde37764e8cf` pending with charge refs. Patched `app/api/run-payout/route.ts` so transfer failures now return `409 transfer_failed` and persist failed state; verified on payout `5b4e91c1-db4f-45e7-ad78-2364683579fd`.
- Verified direct conversion sequencing for the test pair: seeded approved affiliate request, inserted conversion event `a7b014c1-4381-43ea-bc6f-450ab640f24e`, and `POST /api/process-conversion` created exactly one pending payout `d7ba2a74-5230-462e-bc16-a01e035775e5` for 22 AUD at 22% commission; repeat call returned `already_processed` with payout count still 1.
- Verified direct ad-spend settlement sequencing for the test pair: settling live ad `d7b76986-bf98-40aa-a34e-4fd3a9c711c6` created deduction `71629177-ac7f-4b5f-b030-3f4dd160af99`, advanced `spend_transferred` from 0 to 5, and canonical wallet math dropped available balance from 23.82 to 18.82; repeat settlement was idempotent and created no extra deductions.
- Important nuance from the test-pair audit: `wallets.balance` remained 23.82 after the ad-spend deduction, so any UI or logic that trusts the cached wallet row instead of canonical ledger snapshot will drift.
- Open blocker for full launch confidence: successful outbound Stripe transfers (affiliate payouts and business ad-spend transfers) still require sufficient Stripe test-mode available balance to exercise the success path end-to-end.
- Open hardening target: run one real Checkout-backed top-up and one real refund against that top-up to fully verify the patched `cs_...` refund path against live Stripe objects rather than synthetic webhook data.
- Built a local/internal money-flow harness at `app/internal/money-flow/page.tsx` backed by `app/api/internal/money-flow-harness/route.ts`; it is disabled in production by default unless `ENABLE_MONEY_FLOW_HARNESS=true` and supports harness state, Stripe test-mode server-side top-up simulation, affiliate approval seeding, live-ad creation, spend bumping, ad-spend settlement, conversion triggering, payout execution, and refund attempts for the canonical pair `biz@testuser.com` / `affiliate@testuser.com`.
- Verified the new harness end-to-end enough for practical use: `/internal/money-flow` returns 200, `simulate_topup` now credits wallet using a real Stripe test PaymentIntent id (`pi_3TWBsyApch9uT1F00oz1f6dX` in one probe), `settle_ad_spend` via harness charged 3 AUD and reduced canonical available balance from 25.27 to 22.27 with deduction `e5b430f2-182d-4a66-a5be-1dca8e8b41ec`, `trigger_conversion` created payout `a61efb30-b4c0-497c-ba84-da2d6754bbfe`, and harness `run_payout` correctly surfaced `409 transfer_failed` while marking that payout failed.
