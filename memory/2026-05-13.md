# 2026-05-13

- Fixed organic social campaign UX issues in `~/repos/Affliya`:
  - `app/business/my-business/post-ideas/page.tsx` now creates affiliate inbox notifications when organic posts are approved/rejected.
  - `app/affiliate/inbox/page.tsx` now also derives approved/rejected `organic_posts` directly so inbox alerts still appear even if notification inserts are flaky, while avoiding duplicates when notifications exist.
  - `app/affiliate/dashboard/manage-campaigns/page.tsx` “Open post” now routes to `/affiliate/dashboard/manage-campaigns/${item.id}` instead of opening the wrong destination.
- Simplified the business onboarding tracking-install UI in `app/business/setup-tracking/SetupTrackingContent.tsx` into a calmer 3-step flow: choose offer, install code, verify tracking.
- The tracking setup page now reduces clutter, keeps Shopify vs non-Shopify instructions separate and simpler, preserves copy/send-to-developer/test actions, and moves Meta Pixel into a clearer optional section.
# 2026-05-13

- Fixed organic social campaign UX issues in `~/repos/Affliya`:
  - `app/business/my-business/post-ideas/page.tsx` now creates affiliate inbox notifications when organic posts are approved/rejected.
  - `app/affiliate/inbox/page.tsx` now also derives approved/rejected `organic_posts` directly so inbox alerts still appear even if notification inserts are flaky, while avoiding duplicates when notifications exist.
  - `app/affiliate/dashboard/manage-campaigns/page.tsx` “Open post” now routes to `/affiliate/dashboard/manage-campaigns/${item.id}` instead of opening the wrong destination.
- Simplified the business onboarding tracking-install UI in `app/business/setup-tracking/SetupTrackingContent.tsx` into a calmer 3-step flow: choose offer, install code, verify tracking.
- The tracking setup page now reduces clutter, keeps Shopify vs non-Shopify instructions separate and simpler, preserves copy/send-to-developer/test actions, and moves Meta Pixel into a clearer optional section.
- Added a reusable Framer Motion onboarding tour foundation for the business setup flow:
  - `components/onboarding/InteractiveTour.tsx`
  - `data/businessSetupTour.ts`
  - `public/tours/business-setup/README.md`
- User paused further code changes and shifted to Framer help; they want step-by-step guidance and prefer to mark click targets/order themselves before animation polish.
- Agreed workflow for Framer: user uploads/organizes numbered screenshots first, then adds rough rectangles for click targets, then I handle the animation/polish pass.
- In the Telegram/OpenClaw browser-assisted Framer session, the first batch of 10 numbered screenshots was sent for import into Framer so the user can continue the rectangle-marking pass afterward.
